apiVersion: v1
kind: Service

metadata:
  name: frontend
  # namespace del proyecto
  namespace: innovatech

spec:
  selector:
    app: frontend
  ports:
  # puerto publico
  - port: 80
    # puerto nginx
    targetPort: 80
  type: LoadBalancer